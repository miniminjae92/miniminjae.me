# AGENTS.md

## Agent skills

### Issue tracker

이슈와 PRD는 GitHub Issues(`miniminjae92/miniminjae.me`)에 `gh` CLI로 기록합니다. `docs/agents/issue-tracker.md` 참고.

### Triage labels

기본 5개 표준 라벨(`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`)을 그대로 사용합니다. `docs/agents/triage-labels.md` 참고.

### Domain docs

single-context — 루트 `CONTEXT.md` + `docs/adr/`. `docs/agents/domain.md` 참고.
