---
title: minjae.space
slug: minjae-log
date: 2025-11-12
summary: 글이 블로그 세 군데에 흩어져 있었다. 한곳으로 모으고, 쓰는 동안 걸리던 것들을 직접 고칠 수 있게 만들었다.
role: 개인 · 설계 및 구현
visibility: public
stack:
  - Next.js
  - TypeScript
  - Tailwind CSS
  - Velite
  - MDX
  - Vercel
tags:
  - Next.js
  - MDX
# 한 문단은 반드시 한 줄로 쓴다 (whitespace-pre-line 렌더).
problem: |
  글을 네이버 블로그, 티스토리, velog 세 군데에 나눠 쓰고 있었다. 어디에 뭘 썼는지 나부터 기억을 못 했고, 남에게 보여줄 때도 링크를 세 개 보내야 했다. 79편이 그렇게 흩어져 있었다.

  쓰는 동안 걸리는 것도 플랫폼마다 달랐다. 수식이 제대로 안 나오거나, 코드 블록이 마음에 안 들거나, 이미지를 넣으면 글과 따로 놀았다. 어느 것도 내가 고칠 수 있는 게 아니었다.

  직접 만들기로 하고 나서는 다른 문제가 생겼다. 어떻게 보일지부터 정하고 시작하는 바람에 만든 것을 계속 버렸다.
judgment:
  - 글의 형식을 먼저 정하지 않고, 글을 먼저 쓴 다음 그 모양에 맞춰 규칙을 만들었다. 반대로 하면 쓰지도 않을 항목까지 미리 만들게 되고, 그 빈칸을 채우려고 없는 내용을 지어내게 된다.
  - 직접 그린 디자인 시안 여섯 개를 전부 버렸다. 손으로 그린 그림과 장식용 움직임, 화면마다 다른 색을 쓴 게 원인이었다. 보기에는 화려한데 정작 글이 안 읽혔다. 그 뒤로는 색을 아예 쓰지 않고 글자 밝기 네 단계로만 위계를 만든다.
  - 사이트 전체를 하나의 비유로 묶어 두었던 구조를 통째로 지웠다. 그 구조를 남기고 한 페이지만 예외로 두는 선택지도, 새로 고친 페이지를 원래대로 되돌리는 선택지도 있었다. 둘 다 버린 이유는 같다. 무엇을 담을지 정하기 전에 어떻게 보일지부터 정한 것이 반복해서 실패한 원인이었다.
  - 글이 열댓 편뿐인 지금을 기준으로 “만들지 않을 것”부터 정했다. 글끼리 이어 놓은 관계도는 연결이 없다는 사실을 정확하게 그리게 되고, 월별 활동 그래프는 18개월 중 10개월이 비어 있고, 실력 게이지는 비교할 기준이 없다. 대신 날짜 하나만 남겼다. 모든 글에 있고, 쓸수록 좋아지고, 절대 비지 않는다.
  - 글끼리 거는 링크에 오타가 있으면 배포가 아예 안 되게 막았다. 형식 검사만으로는 없는 글을 가리키는 링크를 잡지 못한다. 그냥 두면 링크가 조용히 사라지고 아무도 모른다. 그래서 만드는 단계에서 어느 파일의 어느 줄인지 짚고 멈추게 했다.
metrics:
  - label: 한곳으로 모은 글
    value: 79편 → 14편 공개
    note: 티스토리 38, velog 22, 네이버 19편을 전부 가져왔다. 다시 읽어서 지금도 쓸모 있는 것만 발행하고 나머지는 초안으로 둔다. 발행했다가 다시 내린 것도 있다.
  - label: 버린 디자인 시안
    value: 6종
    note: 2026-07-25에 전부 폐기. 손으로 그린 그림, 장식용 움직임, 화면마다 다른 색이 원인이었다. 지금은 색 없이 글자 밝기 네 단계만 쓴다.
  - label: 만들지 않기로 한 화면
    value: 3종
    note: 글 18편·태그 8개라는 실제 숫자를 보고 정했다. 글 관계도(연결이 없음), 월별 활동 그래프(18개월 중 8개월 공백), 실력 게이지(비교 기준 없음).
writings:
  - optimize-img-video-workflow
  - mdx-special-characters
links:
  - label: 소스 코드
    href: https://github.com/miniminjae92/miniminjae.me
---

{/*
  스텁에서 바꾼 것.

  date: 2024-10-04 → 2025-11-12.
  이 저장소의 첫 커밋 날짜다. 2024-10-04 는 블로그를 쓰기 시작한 시점이지
  이 사이트를 만들기 시작한 시점이 아니다. 이 값이 목록 정렬 축이다.

  writings 두 편은 실제로 연결되는 유일한 항목이다. 양방향 링크가 작동하는
  걸 확인할 수 있는 자리라 남겼다.

  2026-08-05: markdown-style-test 를 비롯한 얇은 메모 4편을 .drafts/ 로
  내렸다. 픽스처와 개인 치트시트라 Writing 의 평균을 끌어내리고 있었다.
  글 18편 → 14편. 되살리려면 .drafts/memo-* 를 memo/ 로 옮기면 된다.
*/}

이 페이지가 이 항목의 증거다. 지금 보고 있는 화면이 그 결과물이고, 아래
`Writing` 칸에 붙은 두 편은 이 사이트를 만들다가 나온 기록이다. 그 두 글을 열면
글 끝에 “이 글은 minjae.space 작업 중 나온 기록입니다”가 자동으로 붙는다. 연결은
한쪽에만 적어 두고 반대쪽은 그때그때 만들어 낸다 — 양쪽에 다 적으면 언젠가
반드시 어긋나기 때문이다.

눈에 안 보이는 쪽에서는 규칙이 글을 검사한다. 제목이 빠졌거나 날짜 형식이 틀렸거나
없는 글을 가리키는 링크가 있으면 배포가 멈추고 어느 파일의 몇 번째 줄인지 알려
준다. 글이 늘어도 깨진 글이 조용히 섞이지 않는다. 수식과 코드 색깔처럼 플랫폼에서
못 고치던 것들도 여기서는 한 곳만 바꾸면 밝은 화면과 어두운 화면에 동시에 반영된다.

아직 안 한 것도 그대로 적어 둔다. 주소를 내 도메인으로 옮기는 것, 글 주소 체계를
정리하는 것, Agent OS와 dref를 화면에서 실제로 연결하는 것. 안 한 것을 안 했다고
적어 두는 편이 계획표를 그럴듯하게 채우는 것보다 낫다.
